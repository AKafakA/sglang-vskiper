# Reference equivalence checks

`ab_compare.py` compares the label and post-traffic attestation format emitted
by the historical `box_vpcov_arm.sh` probe. It remains available for inspecting
those artifacts.

For maintained experiment checks and numerical reproduction, see the
[gate map](../../docs/gates.md). Keep the exact source revision and complete
reports with each run's artifacts.
